# 检查"该考试暂无监控视频"问题的步骤

## 可能的原因和解决方案

### 1. 数据库表未创建
**检查方法：**
- 打开数据库管理工具（如Navicat、MySQL Workbench）
- 连接到数据库 `online_exam`
- 检查是否存在 `exam_video` 表

**解决方法：**
如果表不存在，执行以下SQL：
```sql
CREATE TABLE `exam_video` (
  `videoId` int(0) NOT NULL AUTO_INCREMENT COMMENT '视频编号',
  `examCode` int(0) NULL DEFAULT NULL COMMENT '考试编号',
  `studentId` int(0) NULL DEFAULT NULL COMMENT '学号',
  `videoPath` varchar(255) CHARACTER SET utf8 COLLATE utf8_bin NULL DEFAULT NULL COMMENT '视频文件路径',
  `videoName` varchar(255) CHARACTER SET utf8 COLLATE utf8_bin NULL DEFAULT NULL COMMENT '视频文件名',
  `captureTime` varchar(20) CHARACTER SET utf8 COLLATE utf8_bin NULL DEFAULT NULL COMMENT '捕捉时间',
  `duration` int(0) NULL DEFAULT NULL COMMENT '视频时长（秒）',
  `uploadTime` varchar(20) CHARACTER SET utf8 COLLATE utf8_bin NULL DEFAULT NULL COMMENT '上传时间',
  PRIMARY KEY (`videoId`) USING BTREE,
  INDEX `idx_exam_student`(`examCode`, `studentId`) USING BTREE
) ENGINE = InnoDB AUTO_INCREMENT = 1 CHARACTER SET = utf8 COLLATE = utf8_bin COMMENT = '考试监控视频表' ROW_FORMAT = Dynamic;
```

### 2. 学生端未上传视频
**检查方法：**
- 确认学生是否已经进入考试（非练习模式）
- 检查浏览器控制台是否有错误信息
- 检查学生是否允许了摄像头权限
- 检查后端日志，看是否有视频上传的请求

**解决方法：**
- 确保学生进入的是正式考试（不是练习模式）
- 确保浏览器允许摄像头权限
- 检查网络连接是否正常

### 3. API请求被拦截
**已修复：** 已在 `MvcConfig.java` 中将 `/exam/video/**` 和 `/videos/**` 添加到拦截器排除列表

### 4. 前端API调用问题
**检查方法：**
- 打开浏览器开发者工具（F12）
- 查看 Network 标签页
- 查看是否有 `/api/exam/video/{examCode}` 的请求
- 查看请求的响应内容

**调试信息：**
前端已添加了详细的console.log输出，可以在浏览器控制台查看：
- 请求的考试编号
- API响应内容
- 视频列表数据

### 5. 测试步骤

1. **测试数据库连接：**
   ```sql
   SELECT * FROM exam_video WHERE examCode = 20230001;
   ```
   如果表不存在或没有数据，说明问题在数据库或学生端上传

2. **测试后端API：**
   在浏览器直接访问：
   ```
   http://localhost:9201/exam/video/20230001
   ```
   应该返回JSON格式的视频列表

3. **测试学生端上传：**
   - 以学生身份登录
   - 进入正式考试（非练习）
   - 允许摄像头权限
   - 等待几秒钟，系统会自动录制并上传视频
   - 检查浏览器控制台是否有上传成功的日志

4. **检查视频文件：**
   检查后端项目目录下是否有 `videos` 文件夹
   路径应该是：`./videos/{examCode}/{studentId}/xxx.webm`

### 6. 常见问题

**Q: 为什么学生端没有自动录制？**
A: 
- 检查是否在正式考试模式（不是练习模式）
- 检查浏览器是否允许摄像头权限
- 检查浏览器控制台是否有JavaScript错误

**Q: 视频上传失败？**
A:
- 检查后端服务是否正常运行
- 检查 `application.properties` 中的 `video.upload.path` 配置
- 检查磁盘空间是否充足
- 检查文件权限

**Q: 前端显示"加载视频失败"？**
A:
- 检查后端服务是否启动（端口9201）
- 检查前端代理配置是否正确（`config/index.js`）
- 检查浏览器控制台的错误信息

